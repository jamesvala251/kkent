name: Deploy to Hostinger

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'deploy/**'
      - '.github/workflows/deploy-hostinger.yml'

  workflow_dispatch:

concurrency:
  group: hostinger-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build production frontend
        run: |
          cd frontend
          npm ci
          cd ..
          chmod +x deploy/build-production.sh
          ./deploy/build-production.sh

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.HOSTINGER_SSH_KEY }}

      - name: Add Hostinger host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p "${{ secrets.HOSTINGER_SSH_PORT || '65002' }}" -H "${{ secrets.HOSTINGER_SSH_HOST || '187.124.102.138' }}" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy to Hostinger
        env:
          HOSTINGER_SSH: ${{ secrets.HOSTINGER_SSH_USER || 'u255158670' }}@${{ secrets.HOSTINGER_SSH_HOST || '187.124.102.138' }}
          HOSTINGER_SSH_PORT: ${{ secrets.HOSTINGER_SSH_PORT || '65002' }}
          HOSTINGER_HOME: ${{ secrets.HOSTINGER_HOME || '/home/u255158670' }}
        run: |
          chmod +x deploy/upload-hostinger.sh
          ./deploy/upload-hostinger.sh all
